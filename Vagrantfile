#Vagrant.configure("2") do |config|
#    config.vm.provider "docker" do |d|
#    d.image = "ghost_dev_env"
#    d.ports = ["80:2368"]
#  end
#end

Vagrant.configure("2") do |config|
  config.vm.box = "geerlingguy/centos7"
end
